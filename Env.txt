ACCOUNT_ID: <INSERT ACCOUNT ID>
REGION: <INSERT AWS REGION>
REPOSITORY_NAME: <INSERT ECR REPOSITORY>

Add these in buildspec.yml file:

env:
  variables:
    ACCOUNT_ID: <INSERT ACCOUNT ID>
    REGION: <INSERT AWS REGION>
    REPOSITORY_NAME: <INSERT ECR REPOSITORY>

Or use CodeBuild Environment Variables to add these as key-value pairs.
